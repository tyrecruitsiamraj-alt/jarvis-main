# ส่งต่อเซสชัน — jarvis / So Recruit (7 ส.ค. 2026)

อ่านไฟล์นี้ก่อนไฟล์อื่น แล้วค่อยเจาะรายละเอียดตามลิงก์ข้างล่าง
เขียนให้เซสชันใหม่ทำงานต่อได้ทันทีโดยไม่ต้องไล่ประวัติแชต

---

## 0. สถานะ ณ ตอนส่งต่อ

| | |
|---|---|
| branch | `main` · sync กับ remote แล้ว · **working tree สะอาด** |
| commit ล่าสุด | `c74d900` |
| deploy | ผ่านหมดทุก commit ของวันนี้ (GitHub Actions ~1 นาที) |
| migration | **067–071 รันบน production แล้ว** |
| `npm run test` | **561 ผ่าน / 4 skipped** |
| `npx tsc --noEmit` | สะอาด 0 error **ทั้ง `tsconfig.json` และ `tsconfig.app.json`** |
| `npx eslint .` | 0 error · 16 warning เดิม (MatchingPage · PreCheckPage · 2 context) |

ตัวเลขพวกนี้คือ baseline — เทสต์ตกจาก 561 หรือ tsc มี error = ของใหม่พัง ไม่ใช่ของเดิม

---

## 1. อ่านต่อที่ไหน

| เรื่อง | ไฟล์ |
|---|---|
| **แผนที่ไฟล์ + กับดักทุกข้อ** (สำคัญที่สุด) | `.claude/skills/request-control-tower-advisor/references/09-editing-map.md` |
| แผน Lumos Hybrid + สิ่งที่ทำแล้ว | `docs/lumos-hybrid-flow.md` |
| งาน UI redesign (mockup rev.3) | `docs/ui-redesign-handoff.md` |
| กติกาโดเมน Control Tower | `.claude/skills/request-control-tower-advisor/references/*` |
| mockup "รับไปโทรเอง" ที่เจ้าของเคาะแล้ว | https://claude.ai/code/artifact/44338746-c053-4c70-8130-656e9c21a3ac |

`CLAUDE.md` บังคับให้อ่าน skill references ก่อนแตะงาน Control Tower — ทำตามนั้น

---

## 2. ระบบ Lumos Hybrid — ภาพรวมที่สร้างเสร็จแล้ว

โจทย์เดิมของเจ้าของ: *"ตอนนี้ยังต้องมีคนอยู่ใน flow แต่อนาคตถ้าจะเปลี่ยนไป Auto ล้วนก็จะได้ไม่ยาก"*

```
ใบขอเข้า → AI แมทคนของเรา / ค้น iRecruit
   ↓  (โหมด: manual | assist | auto — สลับที่ Settings ไม่ต้องแก้โค้ด)
ชุดส่งงาน → อนุมัติ → [ถอนคำได้ 10 นาที] → คิว Lumos
   ↓
Lumos โทร → ผลกลับ → นโยบายตัดสินเอง:
   สนใจ            → ปิดเรื่อง ส่งต่อเจ้าหน้าที่
   ไม่สนใจงานนี้    → ปิดแค่ใบนี้ (ใบอื่นยังเสนอได้)
   ไม่หางานแล้ว     → พักเบอร์ 30 วัน ดับทุกใบ
   ขอเลื่อน         → นัดใหม่ตามเวลาที่บอก / +4 ชม.
   ไม่รับสาย        → โทรซ้ำ 3 ครั้ง เว้น 24 ชม.
   ครบเพดาน/เบอร์ผิด → 🚩 ถัง "ต้องคนตาม"
   ↓
คนกด "รับไปตาม" ที่หน้า Follow → โทรที่ "โทรของฉัน" → กดผล → วนกลับเข้าท่อเดิม
```

**หลักคิดที่ยึดตลอด: ท่อเดียว โหมดเป็นแค่สวิตช์** — ผลจาก AI กับผลที่คนกดใช้ศัพท์
`outcome` ชุดเดียวกัน เดินนโยบายตัวเดียวกัน funnel จึงนับรวมกันได้ ไม่แยกไซโล

### หน้าจอที่เกี่ยวข้อง

| หน้า | ทำอะไร |
|---|---|
| Matching (การ์ดผู้สมัคร) | ติ๊กส่ง AI · **"รับไปโทรเอง"** 4 สถานะ · แผงโทรกางในการ์ด · ชิปคัดกรอง |
| `/matching/my-calls` | ถังโทรของฉัน — คิวเรียงให้ + กดผล + visual ว่าผลไหนไปไหน |
| `/matching/call-team` | บอร์ดหัวหน้า (supervisor+) — โอนงาน · คืน AI · เทกอง |
| Follow | funnel การโทร 6 ช่อง + ถัง "ต้องคนตาม" + ชุดส่งรออนุมัติ |
| Settings › โหมดส่งงานให้ Lumos | สวิตช์ manual/assist/auto ต่อจุด (admin) |

---

## 3. ⚠️ กติกาที่ห้ามพลาด (เจ็บมาแล้วทั้งนั้น)

1. **ล็อกโทรผูกกับ "เบอร์ E.164" ไม่ใช่ `candidate_ref`** — คนเดียวมีหลายรหัส
   (บอร์ด `card_id` · iRecruit `id` · Follow `follow-<id>`) แต่เบอร์ที่ดังมีเบอร์เดียว
   ข้อมูลจริง: card 1805 อยู่ในผลแมท **113 ใบขอ** — ไม่ล็อกที่เบอร์ = โทรถล่มคนเดียวกัน
2. **DB เป็นคนตัดสินว่าใครชนะ ไม่ใช่ลำดับโค้ด** — partial unique index + อ่าน unique violation
   ห้ามเปลี่ยนไปเช็คก่อนแล้วค่อย insert (race ได้)
3. **`insertQueueItems()` เป็นคอขวดเดียวของการเข้าคิวทุกเส้น** — กรองล็อก/พักเบอร์ที่นั่นที่เดียว
4. **ตั้งคิวโทรซ้ำต้องรีเซ็ต `result` + `delivery_count`** ไม่ใช่แค่ `status='pending'`
   ไม่งั้น `takePendingLumosItems()` ไม่หยิบแถวนั้นอีกเลย
5. **fail-safe ไปทาง manual/ไม่ส่งเสมอ** — อ่านโหมดไม่ได้ → manual · อ่านรายการพักเบอร์ไม่ได้ → ไม่ส่ง
   เพราะเผลอโทรหาคนที่บอกว่าเลิกหางานแล้ว **กู้คืนไม่ได้**
   (ต่างจากล็อกโทรที่อ่านไม่ได้แล้วส่งต่อ — เสี่ยงโทรซ้ำ < เสี่ยงงานไม่วิ่ง)
6. **ห้ามโทร 20:00–08:00** — `shiftOutOfQuietHours()` (เลข 24 ชม. มักตกตอนตี 2 พอดี)
7. **`new Intl.*` ประกาศระดับโมดูลเสมอ** — เคยทำให้ API ช้า 4.7 วินาที มีเทสต์คุมความเร็ว
8. **สีที่มีความหมายต้องมาจาก `TONE`** ห้ามประกาศตารางสีสถานะในไฟล์หน้า (มีเทสต์บังคับ)
9. **ห้ามใช้ `text-foreground` บนพื้นเข้ม/glass** — ใช้ `DASH.cell*`
10. **DB local = production** — ทดสอบอะไรที่เขียนต้องคืนค่าเดิม
11. **dev server ใช้ `preview_start`** (`vite` 8080 · `api` 3100) ห้ามรันด้วย Bash
    · auth ทดสอบ: `POST /api/auth/dev-role {"role":"admin"}` (cookie ~30 นาที)
12. **`verify-api-registry.mjs` ต้องรันด้วย `npx tsx` ไม่ใช่ `node`**

---

## 4. 🔴 5 เรื่องที่ต้องให้เจ้าของตัดสิน (อย่าเดาแทน)

เซสชันใหม่ควรถามให้จบก่อนลงมือข้อที่เกี่ยวข้อง

### 4.1 ความหมายสีที่ขัดกับ token (3 จุด)
| จุด | ตอนนี้ | ปัญหา |
|---|---|---|
| ชิป Scraping | **เปลี่ยนเป็น teal ไปแล้ว** | token ล็อก teal = Scraping แต่ของเดิมเป็นเขียว · เอาเขียวคืนไหม |
| `ติดใบขอ` | ส้ม | token ล็อกส้ม = "ส่งทีมคอนเทนต์" · `warn`/`violet` น่าจะตรงกว่า |
| "AI แนะนำ n" | เขียว | token บอก info(ฟ้า) = AI แนะนำ · แต่เปลี่ยนแล้วจะชนกับ "น่าจะตรง ~n" ที่เป็นฟ้า |

### 4.2 ค่าที่ตั้งเองไปก่อน (ยังไม่ยืนยัน)
| ค่า | ตั้งไว้ | แก้ที่ |
|---|---|---|
| ช่วงถอนคำหลังอนุมัติ | 10 นาที | `CALL_BATCH_UNDO_MINUTES` ใน `src/lib/callBatch.ts` |
| ใครอนุมัติชุดส่งได้ | supervisor/admin | `canApprove()` ใน `api/_handlers/lumos-call-batches.ts` |

*(ที่เจ้าของเคาะแล้ว: ล็อกโทร **1 วัน** · "ไม่สนใจ" แยก 2 แบบ · โทรซ้ำ 3 ครั้ง/24 ชม.)*

### 4.3 แผงสีม่วง/น้ำเงินใน MatchingPage (~40 จุด)
สีใช้เป็น "ตัวแบ่งกลุ่ม" ของแผง ไม่ใช่ความหมายใน 9 โทน — ต้องตกลงก่อนว่ากลุ่มควรมีโทนไหม

### 4.4 iRecruit ต่อผลคัดกรอง
`CandidateChecklist` ฝั่ง iRecruit ยังไม่ส่ง `screening` เพราะยังไม่ชัดว่าผูก ref ด้วยอะไร
(ฝั่งบอร์ดใช้ `card_id`) — ถ้าใช้ `id` ของ iRecruit ได้ ทำต่อได้ทันที

### 4.5 เปิด auto จริงเมื่อไหร่
ตอนนี้ **manual ทุกจุด** = production ไม่โทรเอง · เปิดเมื่อพร้อมที่ Settings

---

## 5. งานที่เหลือ (นอกจากข้อ 4)

1. MatchingPage เหลือสีเขียนมือ ~343 จุด (จาก 681) — ที่เหลือส่วนใหญ่เป็นสีโครงสร้าง/ตกแต่งที่ตั้งใจไม่แตะ
2. ปุ่ม outline พื้นขาว 3 ตัวใน MatchingPage — `TONE` ไม่มี variant "outline บนพื้นขาว"
3. หน้ารายละเอียดใบขอ/ผู้สมัครรายคน ยัง audit สีไม่ถึง
4. UI สร้างชุดส่งจากหน้า Matching โดยตรง (ตอนนี้ชุดเกิดจากโหมด assist เท่านั้น · API รองรับสร้างเองแล้ว)

---

## 6. สไตล์ที่เจ้าของชอบ (สำคัญ — ทำตามนี้)

- สั่งเป็น bullet ไทยสั้น ๆ ทีละหลายข้อ → **ทำครบทุกข้อแล้วค่อยสรุปทีเดียว**
- **ตรวจงานเองในเบราว์เซอร์เสมอ** อย่าให้เจ้าของไปกดเอง แล้วรายงานสิ่งที่วัดได้จริง
- **ห้าม push จนกว่าจะสั่ง** — ยกเว้นเจ้าของบอก "ทำทีละข้อ Commit&Push ทีละข้อ" (เคยสั่งแบบนี้ = อนุญาตต่อเนื่อง)
- commit แยกก้อนตามเรื่อง · message ยาวได้ บอกเหตุผลและสิ่งที่ตรวจ
- เจ้าของบอก "ทำแบบเดิม" → **เช็ค git history ก่อนว่าเคยมีจริงไหม**
- เอาฟีเจอร์ออก = เอาออกให้สุด (UI + state + URL param) ไม่ใช่แค่ซ่อน
- ตอบภาษาไทย · ถ้าเจ้าของขอ "แบบ caveman" คือสั้น ตรง ไม่อ้อม

---

## 7. บทเรียนจากเซสชันนี้ (กันพลาดซ้ำ)

- **อย่าเชื่อ docstring** — เคยรายงานว่า `verify-api-registry.mjs` พัง เพราะรันตามที่ docstring บอก
  (`node`) จริง ๆ ต้อง `tsx` · ตรวจวิธีเรียกใช้จริงใน CI/workflow ก่อนสรุปว่าอะไรพัง
- **ทดสอบกับข้อมูล production จริงเสมอ** — funnel เคยโชว์ "มีผลกลับ 458 แต่โทรติด 0"
  เพราะคอลัมน์ใหม่ยังว่างในแถวเก่า เทสต์ mock จับไม่ได้
- **`innerText` อ่านไม่ได้ถ้ามี `content-visibility: auto`** — ใช้ `textContent` ตอนตรวจ DOM
- **`mockImplementation` แบบค้างทำให้ vitest รายงาน error ของ mock เป็น failure ของเคสนั้น**
  ใช้ `mockImplementationOnce`
- แก้หลายไฟล์ติดกันแล้ว `tsx watch` อาจ reload กลางคัน → log บอก export หายทั้งที่มีจริง · รีสตาร์ท server
