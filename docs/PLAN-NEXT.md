# แผนงานต่อ — Fable วางแผน · Opus ลงมือ (7 ส.ค. 2569 เย็น)

**วิธีทำงาน (เจ้าของสั่ง):** ทำทีละข้อ → ตรวจ → commit → push → ข้อถัดไป จนครบ
อ่านก่อนเริ่ม: `docs/SESSION-HANDOFF.md` (กติกา 12 ข้อ) และ
`.claude/skills/request-control-tower-advisor/references/09-editing-map.md`

**Baseline ณ commit `270c2ec`:** test 562 ผ่าน/4 skip · tsc 0 error **ทั้ง `tsconfig.json`
และ `tsconfig.app.json` (ต้องเช็คทั้งคู่!)** · eslint 0 error/16 warning เดิม · migration ถึง 072

**กติกาเหล็ก:** DB local = production (ทดสอบแล้วคืนค่า) · dev server ใช้ preview_start
(vite 8080 / api 3100 · auth: `POST /api/auth/dev-role {"role":"admin"}`) ·
ตรวจเองในเบราว์เซอร์เสมอ · สีมาจาก TONE เท่านั้น · commit message ไทย บอกเหตุผล+สิ่งที่ตรวจ

---

## งานที่ทำได้เลย (เรียงตามลำดับ ทำทีละข้อ)

### 1. เทสต์กันฐานตัวเลขถอย (เป้า: ตัวเลข 7→10)
- สร้าง `src/lib/callFunnelMath.ts` — ฟังก์ชัน pure: `resolvedCallBase(funnel)` =
  withResult − byOutcome.cancelled · `conversionRates(funnel)` คืน {connected, confirmed, needsHuman}%
- ย้ายการคำนวณใน `CallFunnelPanel.tsx` (บล็อก "จากสายที่มีผลจริง") มาเรียกไฟล์นี้
- เทสต์ `tests/api/callFunnelMath.test.ts`: เคสจริงที่เคยพลาด — withResult 458,
  cancelled 409 → ฐานต้อง 49 ไม่ใช่ 458 · ฐาน ≤ 0 ต้องไม่หารศูนย์
- ตรวจ: เปิด /follow เลขเดิมต้องขึ้น (โทรติด 61% ณ ข้อมูลวันนี้)

### 2. เครื่องวัด contrast เป็นสคริปต์ + audit หน้าที่เหลือ (เป้า: สองธีม 8→10)
- เอาเครื่องวัดที่ถูกต้องจาก commit `19842f3` (อยู่ในข้อความ commit/โค้ดที่เคยรันในเบราว์เซอร์:
  **ซ้อนสีโปร่งใสจากล่างขึ้นบน** · เกณฑ์ 4.5 ปกติ / 3.0 ตัวใหญ่≥24px หรือหนา≥18.66px ·
  ข้าม aria-hidden/ซ่อน) เขียนเป็น `scripts/audit-contrast.mjs` รันผ่าน browser tool
- audit: /follow · /dashboard · /matching/my-calls (สลับ role admin) · /jobs/list ทั้งสองธีม
- แก้ที่ตกเกณฑ์ด้วยแนวเดิม: พื้นอ่อนไม่มีคู่ dark → เติม `dark:bg-*` · สีสถานะ → TONE
- ⚠️ ข้อยกเว้นที่เจ้าของเคาะแล้ว: ปุ่ม CTA "ให้สร้าง Content" (ส้ม) กับ "Scraping งาน" (teal)
  ใน MatchingPage — **ไว้งั้นแหละ ห้ามแตะ**
- commit แยกต่อหน้า

### 3. เลขประมาณ vs เลขยืนยัน แยกด้วยตา (เป้า: ป้าย 6→10 ครึ่งแรก)
- จุดหลัก MatchingPage: ชิป "AI แนะนำ n" (ยืนยันแล้ว) vs "น่าจะตรง ~n" (เดาจากสกิล)
- ทำเลขประมาณให้ต่างด้วยรูปแบบ ไม่ใช่แค่ `~`: เส้นขอบประ (`border-dashed`) + จางกว่า
  ทุกที่ที่โชว์เลขประมาณ (การ์ดใบขอ · แถบสรุปในแผง) — หา comment "ประมาณการ" ในไฟล์
- เพิ่ม title/tooltip อธิบายทุกจุด · ตรวจสองธีมในเบราว์เซอร์

### 4. ยุบภาษาความด่วนเหลือชุดเดียว (ป้าย 6→10 ครึ่งหลัง)
- ตอนนี้การ์ดใบขอมี 2 ระบบซ้อน: ชิป "ด่วน/ล่วงหน้า" (urgency 2 ค่า) + ถังอายุ 4 ระดับ
  (`JOB_AGE_URGENCY_META`) — ให้เหลือถังอายุ 4 ระดับเป็นหลัก
- ชิป "ด่วน/ล่วงหน้า" ใน MatchingPage (TONE.danger.chip/info.chip ราวบรรทัด 3030)
  เอาออกเมื่อการ์ดมีชิปอายุอยู่แล้ว — **เอาออกให้สุด ไม่ใช่ซ่อน** (กติกาเจ้าของ)
- ⚠️ ห้ามแตะตรรกะ `computeJobUrgency`/SLA — แตะแค่การแสดงผล · เทสต์เดิมต้องผ่านครบ

### 5. จัดลำดับอ่านการ์ดใบขอ "บนลงล่าง" (หลักที่เจ้าของยึด)
- หลัก: บนสุด = สิ่งที่ต้องตัดสินใจ (ด่วยแค่ไหน · เหลือหากี่คน · มีคนแนะนำไหม) ·
  รอง = ชื่อหน่วยงาน/ตำแหน่ง · ล่าง = เลขที่ใบขอ/วันที่ (ของอ้างอิง)
- ทำเฉพาะการ์ดลิสต์ใน MatchingPage ก่อน แล้ว **ทำ mockup HTML เทียบก่อน-หลังส่งให้เจ้าของดูก่อนแก้จริง**
  (เจ้าของชอบดูภาพก่อน — ดู mockup rev.3 เป็นแบบ)

### 6. แปล skill เป็นไทย
- แปลไฟล์อังกฤษใน `.claude/skills/request-control-tower-advisor/` (SKILL.md + references
  01,02,03,04,05,06,07,08 — 09 เป็นไทยส่วนใหญ่แล้ว) **คงชื่อไฟล์/โฟลเดอร์อังกฤษ**
- ศัพท์ยึดตาม CLAUDE.md (ขอมา/หาได้แล้ว/ปิดครบใบขอ/ยกเลิก/จบงานแล้ว/เหลือหา/งานค้าง)
- ห้ามเปลี่ยนความหมายกติกาแม้แต่ข้อเดียว — แปลอย่างเดียว · commit ทีละไฟล์หรือทีละกลุ่ม

### 7. สารบัญ skill ภาษาไทย
- `docs/SKILLS-INDEX.md`: ตาราง skill ทั้งหมดในเครื่อง — ชื่อ · ใช้ตอนไหน · สั่งยังไง
  ภาษาคนอ่าน ไม่ใช่ dump frontmatter

---

## งานที่ต้องถามเจ้าของก่อน (ห้ามเดาแทน — ถามแล้วค่อยทำ)

| เรื่อง | สถานะ | คำแนะนำของ Fable |
|---|---|---|
| สีชิป 3 จุด (Scraping/ติดใบขอ/AI แนะนำ) | เจ้าของยังไม่เคาะ (บอกว่า "งง" — มีภาพเทียบให้ดูแล้ว) | แก้แค่ `ติดใบขอ` ส้ม→ม่วง อีกสองจุดคงเดิม |
| .codex ลบทิ้งไหม | ยังไม่ตอบว่ายังใช้ Codex อยู่ไหม | ถ้าไม่ใช้แล้ว ลบทั้งก้อน เหลือ .claude ที่เดียว |
| โคลนคนเก่ง | ต้องให้เจ้าของเลือกคน + จัดเวลาถอดวิชา | เริ่มจากคนที่งานคอขวดสุด · โครง: ถอดเคสจริง 10–20 เคส → skill ไทย → ทดสอบเทียบตัวจริง |
| แตก MatchingPage (5,000+ บรรทัด) | หนี้โครงสร้าง — งานใหญ่ เสี่ยงสูง | เสนอแผนแยกก่อน (ชิ้นละ component + เทสต์) อย่าลุยรวดเดียว |

## ที่เจ้าของเคาะแล้ว (อย่าถามซ้ำ)

- ถอนคำ 10 นาที ✓ · อนุมัติชุดส่ง = supervisor/admin ✓ · auto ยังไม่เปิด (manual ต่อ) ✓
- ปุ่ม CTA ส้ม/teal contrast ต่ำ — ไว้งั้นแหละ ✓ · หน้างานโทรซ่อนให้ admin ✓
- iRecruit ผูก ref ด้วย `id` ✓ (ทำแล้ว)

## กับดักที่ Fable เจอมาแล้ววันนี้ (อย่าเหยียบซ้ำ)

1. `npx tsc --noEmit` เฉย ๆ **ไม่ครอบ src/** — ต้อง `-p tsconfig.app.json` ด้วยเสมอ
2. แก้ class หลายจุดด้วย regex/สคริปต์ → เคยพัง 2 ครั้ง (แทรก `${TONE}` ใส่ string ธรรมดา ·
   คอมเมนต์ JSX ผิดที่) — แก้มือทีละจุด หรือสคริปต์แบบ "เติมท้ายอย่างเดียว" เท่านั้น
3. เครื่องวัด contrast ต้องซ้อนสีจากล่างขึ้นบน ไม่งั้น false positive เป็นสิบจุด
4. ตัววัดในเบราว์เซอร์ใช้ได้เฉพาะตอน pane เปิด (viewport=0 เมื่อซ่อน) — screenshot ก่อนวัด
5. แจ้งเตือน: ตัวสร้างกลืน error เสมอ — ห้ามให้มันทำงานหลักล้ม
