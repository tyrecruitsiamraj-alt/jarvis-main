# แผนงานต่อ (อัปเดต 11 ส.ค. 2569)

**วิธีทำงาน (เจ้าของสั่งไว้แล้ว):** ทำทีละข้อ → ตรวจในเบราว์เซอร์ → commit → push → ข้อถัดไป
ไม่ต้องรอถามระหว่างทาง

อ่านก่อนเริ่ม: `docs/SESSION-HANDOFF.md` (กติกา 13 ข้อ + กับดักตอนตรวจงาน + คำตัดสินเจ้าของ
+ **ทางตัน 3 เรื่องที่ต้องเคาะ**) และ
`.claude/skills/request-control-tower-advisor/references/09-editing-map.md`

**Baseline (14 ส.ค. รอบสิบเอ็ด):** test **1,009 ผ่าน/4 skip (113 ไฟล์)** ·
**tsc ต้องเป็น 0 ทั้งสาม config** · eslint 0 error/16 warning ·
verify-api-registry ผ่าน (**77 route**) · migration **บนฐานถึง 083 แล้ว** ·
precompute **เปิดแล้ว** (.env.local — server จริงต้องใส่เองตอน deploy)

## 🔴 มีหมุดวันเปิดใช้จริงแล้ว — อ่าน `docs/ROADMAP.md` ก่อน

| หมุด | วัน | สถานะ |
|---|---|---|
| Follow / Matching / สรรหา ส่ง Lumos โทร | **อังคาร 18 ส.ค. 2569** | ⛔ เหลือบล็อกเดียว: **Lumos เงียบ 9 วัน** (ต้องคุยกับทีมเขา) · ~~คิวค้าง 140 คน~~ ✅ ล้างแล้ว 13 ส.ค. |
| PoC คิด Contents / Scrap | **พฤหัส 20 ส.ค. 2569** | ⛔ ยังไม่มีคนรับงานคอนเทนต์ · คำว่า "Scrap" **เจ้าของสั่งข้ามไปก่อน 13 ส.ค.** (ยังไม่นิยาม) |

**งานพัฒนาข้างล่างนี้มาทีหลังหมุดสองอันนี้** — ถ้าเวลาชนกัน ให้เคลียร์บล็อกก่อน

## 🎯 งานถัดไป (เรียงแล้ว — เคาะครบ ห้ามถามซ้ำ · ความรู้เต็มใน SESSION-HANDOFF §0.15–0.3)

1. ~~**"ที่จอง" ใหม่จากผลโทร "สนใจ"**~~ ✅ **ทำแล้ว 14 ส.ค. 2569**
   · ปุ่ม "จองตัวเลย" อยู่ 2 ที่: dialog รายละเอียดคนจากกล่อง "สนใจงาน" บนหน้าหลัก ·
     แถวที่เพิ่งกดผล "สนใจ" ในถังโทรของฉัน
   · ตรรกะล้วนที่ `src/lib/callResultBooking.ts` (เทสต์ 16 เคส · mutation 7/7)
   · ยิง `saveProposal()` เส้นเดียวกับปุ่มจองหน้า Matching → กติกา "1 คนจองได้ใบเดียว" ยังอยู่
   · ⚠️ **ยังเหลือ**: ตัวจัดการใน MatchingPage (`openIrecruitProposalAction` ฯลฯ)
     ยังไม่มีใครเรียก — ปุ่มจองใน drawer ยังไม่ได้เอากลับ (เจ้าของสั่งถอดไว้รอบแปด)
     ถ้าอยากได้ปุ่มจองใน drawer ด้วยต้องสั่ง
2. ~~**ระบบ Lead**~~ ✅ **ทำแล้ว 14 ส.ค. 2569** · **migration 083 รันบนฐานแล้ว**
   · ปัดแล้วหายจากทุกแท็บ (กรองที่คิวรีฝั่ง server ไม่ใช่ที่หน้าเว็บ)
   · ปุ่ม "ดูคลังสำรอง (Lead)" → `?lead=1` · ในคลังสำรองปุ่ม "เก็บ Lead" ซ่อน เหลือ "ลบ Lead"
   · ตรรกะล้วน `src/lib/recruitLead.ts` (เทสต์ 10 เคส · mutation 6/6)
   · ตรวจกับฐานจริงครบวง (seed ZZ 2 ใบ → ปัด → หาย → คลังสำรองเห็น → เรียกคืน → ลบคืน 0 แถว)
3. ~~**เรียงคิวโทรตาม tier AI**~~ ✅ **ทำแล้ว 14 ส.ค. 2569** · **migration 084 รันบนฐานแล้ว**
   · เขียว 1 · เหลือง 2 · แดง 3 · **ไม่มีคะแนน = 2 (เท่าเหลือง ไม่ใช่ท้ายแถว)**
     — ความหมายอยู่ที่ `src/lib/matchRank.ts` ที่เดียว (เทสต์ 6 เคส)
   · คิวรีเสิร์ฟเรียง `(coalesce(match_rank,2), created_at, id)` ทั้ง ORDER BY และชั้นกันซ้ำ
   · ฐานที่ยังไม่รัน 084 ถอยเป็นคิวรีเดิมอัตโนมัติ (ดัก 42703) ทั้งขาเข้าคิวและขาเสิร์ฟ
   · ⚠️ **ยังไม่ได้วัดผลการเรียงกับข้อมูลจริง** — คิวถูกล้างหมด 13 ส.ค. เหลือ 0 แถวที่รอโทร
     ต้องวัดซ้ำหลังมีคิวใหม่เข้ามา 18 ส.ค.
4. **วันนัดจริงในแท็บติดตามนัดหมาย** — GET /api/candidate-interviews?all=1
   (⚠️ ตาราง candidate_interviews ตอนนี้ **ว่าง 0 แถว** — ทำแล้วคอลัมน์จะยังว่าง
   จนกว่ามีการบันทึกนัดจริง · เช็คกับเจ้าของว่าจะเริ่มบันทึกจากไหน)
5. **deploy จริง**: server ต้องใส่ MATCH_PRECOMPUTE_ENABLED=true ใน .env เอง

### ❌ ตกไปแล้ว (13 ส.ค.)
- ~~ยกใบสมัคร 154,820 ใบจาก iRecruit~~ — เจ้าของประกาศเริ่มเก็บรายชื่อใหม่จาก 0
  ("ไม่ได้ให้เอา database จาก iRecruit มา") · master ที่ยกแล้ว (ช่องทาง 4,388 ·
  เหตุผล 67 · ตำแหน่ง 479) คงไว้ใช้ต่อ

---

## ⛔ ต้องเคาะก่อน (บล็อกงานอื่น)

| เรื่อง | สถานะ |
|---|---|
| ~~ลูปอนุมัติชุดโทรไม่มีที่ไป~~ | ✅ **เคาะแล้ว 11 ส.ค.: ข้ามขั้นอนุมัติ + คงช่วงถอนคำ 10 นาที** — ทำเสร็จแล้ว (`578b8a5`) |
| ~~BU ของผู้สมัคร~~ | ⏸️ **เคาะแล้ว 11 ส.ค.: พักไว้ก่อน** |
| ~~ตั้ง BU ให้บัญชี admin~~ | ✅ **เคาะแล้ว 11 ส.ค.: ปล่อยให้ admin เห็นทุก BU** เป็นพฤติกรรมที่ตั้งใจ |
| **คะแนนแมทที่จะใช้เรียงคิว** | 🆕 รอเคาะ — ดูข้อ 1 ข้างล่าง |

---

## งานที่ทำได้เลย (เรียงตามที่ควรทำก่อน)

### 1. ล็อกคนข้ามใบ + เสนอทีละงาน — **ทำแล้ว 2 ใน 3 ส่วน**

เจ้าของเคาะกติกาไว้ครบแล้ว:
- คนเดียวแมทได้หลายใบ → **เสนอทีละใบ เรียงตามคะแนนแมทของคนนั้น**
- ได้คำตอบว่า "สนใจใบ A" → **บังใบอื่นจนกว่าใบ A จะจบ**

✅ **ทำแล้ว** (`2833d41` + `9cafc7b`) — กัน 3 ชั้นใน `TAKE_PENDING_SQL`:
1. สายกำลังเดิน · 2. ใบที่มาก่อนได้ก่อน · 3. สนใจใบไหนแล้วบังใบอื่น (เพดาน 7 วัน)
วัดกับฐานจริง: reminder ถึงคิว 2,816 แถว → เสิร์ฟ 126 แถว = 126 คน
(เจอบั๊กระหว่างทาง: ล็อก "รับไปโทรเอง" + พักเบอร์ ไม่เคยมีผลกับช่อง interview เลย — แก้แล้ว)

✅ **เรียงตามคะแนนแมทแล้ว 14 ส.ค. 2569** (`match_rank` · migration 084 รันบนฐานแล้ว)
เจ้าของเลือกทาง ก) **tier ของ AI** — เขียว 1 · เหลือง 2 · แดง 3 · ไม่มีคะแนน 2
ความหมายอยู่ที่ `src/lib/matchRank.ts` ที่เดียว · รายละเอียด+กับดักอยู่ใน 09-editing-map
หัวข้อ "เรียงคิวตาม tier ของ AI"

### 2. ~~ติ๊กเลือกคนแล้วอนุมัติเฉพาะที่เลือก~~
❌ **ตกไปแล้ว** — เจ้าของเลือกทางข้ามขั้นอนุมัติ จึงไม่มีขั้นอนุมัติให้ติ๊ก

### 2.5 งานสรรหา (RM) — เหลืออะไร
ดู `docs/RM-HANDOFF.md` §6 · เรียงแล้ว: **ระบบ Lead** (เจ้าของเคาะนิยามแล้ว) →
ปุ่ม action ต่อแถว → บันทึกผลติดต่อ/นัด → วันนัดจริง → master ตำแหน่งงาน →
ยกใบสมัคร 154,820 ใบ

### 3. เปิด precompute AI
`MATCH_PRECOMPUTE_ENABLED` ยังไม่มีใน `.env` เลย = AI ไม่ได้ประเมินใบขอล่วงหน้าจริง
ตัวเลข "AI แนะนำแล้ว" ที่เห็นมาจากผลที่เคยคิดเก็บไว้ ไม่อัปเดตเอง

### 4. เขียนเทสต์คลุมหน้า Matching
`MatchingPage.tsx` **~4,200 บรรทัด ไม่มีเทสต์เลย** — ตรรกะที่มีเทสต์คือ lib ที่มันเรียก
แต่ตัวหน้า (ติ๊กเลือก · สร้างชุด · กรอง · state ที่ผูกกันหลายตัว) พังแล้วรู้ตอนเปิดหน้าอย่างเดียว

### 5. ช่องว่างเทสต์ที่เหลือ
`authSession.ts` (ออก session หลัง SSO) · `roleFunctionGrants.ts` (สิทธิ์รายฟังก์ชัน —
ตัวเดียวกับที่เจอบั๊ก audit) · `magicLinkLogin.ts`

### 6. ดึงเคสให้เจ้าของไปถามคนเก่ง (เมื่อเจ้าของขอ)
วิธีอยู่ `docs/HOW-TO-COLLECT-CASES.md`

---

## ต้องถามเจ้าของก่อน (ห้ามเดาแทน)

| เรื่อง | สถานะ |
|---|---|
| ~~คะแนนที่ใช้เรียงคิว + รัน migration~~ | ✅ **ทำแล้ว 14 ส.ค.: tier ของ AI + migration 084** |
| **เพดาน "บังใบอื่น" 7 วัน** | ผมตั้งไว้เอง เจ้าของยังไม่เคาะ · แก้ที่ `CONFIRMED_FOCUS_DAYS` ใน `src/lib/callFollowupPolicy.ts` ที่เดียว |
| ~~ชุดรออนุมัติค้าง 1 ชุดบนฐาน~~ | ✅ **เคาะแล้ว 11 ส.ค.: ปล่อยค้างไว้** (`siamraj-sql:OPL6908018` · 1 คน) — ไม่มีใครถูกโทรจากชุดนี้ |
| ~~โหมด assist~~ | ✅ **เคาะแล้ว 11 ส.ค.: ถอดทิ้ง** — ไม่มีทางไหนสร้างชุด `pending_approval` ได้อีกแล้ว |
| hard delete `DELETE /api/candidate-interviews` | ทางลบถาวรทางเดียวที่เหลือ — ต้อง migration + ตกลง visibility |
| โคลนคนเก่งให้แม่นขึ้น | ต้องให้เจ้าของเลือกคน + เวลา 1 ชม./คน |

## ที่เคาะแล้ว — ดูตาราง §4 ใน `docs/SESSION-HANDOFF.md` (อย่าถามซ้ำ)
