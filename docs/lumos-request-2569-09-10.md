# คำขอถึงทีม Lumos — 10 กันยายน 2569

> ร่างไว้ให้เจ้าของส่งต่อ · ตัดหัวข้อนี้ทิ้งก่อนส่งได้เลย
> ข้อมูลอ้างอิงทั้งหมดมาจากฐานจริงของ jarvis วันที่ 9–10 ก.ย. 2569
> **ไม่ใส่เบอร์โทรในเอกสารนี้** — ใช้ `client_contact_id` เป็นตัวอ้างอิงแทน

---

เรียนทีม Lumos

รบกวนขอความช่วยเหลือ 2 เรื่องเกี่ยวกับสายเตือน (channel `reminder`, job `follow`) ครับ

## เรื่องที่ 1 — ขอฟิลด์บอกผลการโทรเบอร์ผู้ดูแล (`admin_phone`)

ตอนนี้เราส่ง `admin_phone` ไปกับสายทุกสาย (ครบ 100 จาก 100 รายการที่ตรวจ)
ตามที่ตกลงกันไว้ว่า ถ้า AI ติดต่อผู้รับสายไม่ได้หรือได้คำตอบนอกเหนือจากที่กำหนด
ให้โทรหาเบอร์นี้ต่อ

ปัญหาคือ **ผลที่ส่งกลับมาไม่มีฟิลด์ไหนบอกว่าโทรเบอร์นี้หรือยัง** เราตรวจผลจริง
18 สายที่โทรจบแล้ว พบว่ามี 23 ฟิลด์ ได้แก่

```
recipient_phone, recipient_name, title, tone, language, status, outcome,
summary, message, transcript, recording_url, next_action, ended_reason,
stop_early, call_attempts, plan_id, plan_status, step_id, step_type,
step_position, scheduled_at, event_id, client_contact_id
```

ไม่มีฟิลด์ใดกล่าวถึงการโทรหา `admin_phone` เลย และค้นทั้งก้อน JSON ก็ไม่พบทั้งคำว่า
`admin` และไม่พบหมายเลขของ `admin_phone` ในผลใด ๆ

ผลกระทบฝั่งเรา: หน้าจอติดตามบอกเจ้าหน้าที่ได้แค่ว่า "แนบเบอร์ไปกับสายแล้ว"
แต่บอกไม่ได้ว่าโทรไปหรือยัง ทำให้เจ้าหน้าที่ไม่รู้ว่าต้องโทรเองซ้ำหรือไม่

**สิ่งที่อยากขอ** — เพิ่มข้อมูลส่วนนี้ในผลที่ส่งกลับ รูปแบบตามที่ทีมสะดวก เช่น

```json
"admin_call": {
  "attempted": true,
  "connected": true,
  "at": "2026-09-09T16:59:00+07:00",
  "reason": "recipient_declined",
  "outcome": "acknowledged",
  "recording_url": "..."
}
```

อย่างน้อยที่สุดขอแค่ `attempted` (โทรหรือยัง) กับ `connected` (ติดหรือไม่) ก็พอครับ
ถ้าเคสไหนไม่ได้โทร ขอให้ส่ง `"attempted": false` มาด้วย จะได้แยกออกจากกรณี
"ไม่มีข้อมูล"

## เรื่องที่ 2 — สายจำนวนมากไม่มีผลส่งกลับ อยากทราบสาเหตุ

จากรายการที่เราส่งเข้าไป มีจำนวนไม่น้อยที่ไม่เคยมีผลส่งกลับมาเลย และไม่มี error
แจ้งกลับมาทางฝั่งเราด้วย

| ชุด | ส่งเข้าไป | ได้ผลกลับ |
| --- | --- | --- |
| สายรอบที่ 1 | 51 | 3 |
| สายรอบที่ 2 | 49 | 15 |

เฉพาะวันที่ 9 ก.ย. 2569 เราส่ง 23 รายการ (กระจายอยู่บน 7 หมายเลข) ได้ผลกลับ 10 รายการ

**ข้อสังเกตที่อยากให้ช่วยยืนยัน** — เมื่อแยกดูเป็นรายหมายเลข พบว่าแต่ละหมายเลข
ได้ผลกลับประมาณ 1–3 รายการ ไม่ว่าเราจะส่งเข้าไปกี่รายการก็ตาม เช่น

* หมายเลข A — ส่ง 6 รายการ (รอบ 1 จำนวน 3 · รอบ 2 จำนวน 3) ได้ผลกลับ 1 รายการ
* หมายเลข B — ส่ง 5 รายการ ได้ผลกลับ 3 รายการ
* หมายเลข C — ส่ง 2 รายการ ได้ผลกลับ 1 รายการ

จึงอยากถามว่า **ระบบจำกัดจำนวนแผนการโทรที่ทำงานพร้อมกันต่อหนึ่งหมายเลขหรือไม่**
ถ้าใช่ รายการที่ส่งเข้าไปทีหลังจะไปแทนที่รายการก่อนหน้า หรือถูกปัดทิ้ง
และมีวิธีให้เราส่งหลายสายต่อหนึ่งหมายเลขในวันเดียวกันได้อย่างไร

หมายเหตุ: ข้อมูลชุดนี้เป็นข้อมูลทดสอบที่มีการใช้หมายเลขซ้ำกันหลายรายการ
ซึ่งอาจไม่ตรงกับการใช้งานจริง แต่ในงานจริงเราก็มีกรณีที่ต้องโทรหาคนเดียวกัน
หลายรอบในวันเดียว จึงอยากทราบข้อจำกัดตรงนี้ให้ชัดครับ

**ตัวอย่างรายการที่ส่งเข้าไปแล้วไม่เคยมีผลกลับ** (นัดวันที่ 9 ก.ย. 2569
เวลาตามเวลาประเทศไทย · รบกวนช่วยตรวจว่าฝั่งระบบได้รับและทำอะไรกับรายการเหล่านี้)

| `client_contact_id` | เวลานัด | ส่งเข้าระบบเมื่อ |
| --- | --- | --- |
| `follow-8aa963bd-8589-4f66-a8fe-2cbcd7f8d323` | 16:33 | 16:30 |
| `follow-ba98e085-766f-475a-b675-ae29888724f0` | 16:35 | 16:32 |
| `follow-4b6bd1b9-289b-4b5e-93ec-354a20848702` | 16:35 | 16:31 |
| `follow-1e042f41-e5d9-4333-9069-132e8b74bb3f` | 16:36 | 16:32 |
| `follow-7ec5c556-89f0-423b-a14b-d79bba1919c4` | 16:38 | 16:32 |
| `follow-2d1fed85-5f7c-4dd0-9c82-087d4ad52063` | 16:40 | 16:33 |

**ตัวอย่างรายการที่ทำงานปกติในวันเดียวกัน** ไว้ให้เทียบ

| `client_contact_id` | `plan_id` |
| --- | --- |
| `follow-8f60fa76-5c06-44f1-89b6-1234aaa1403c` | `cbd6b7c4-0d40-4e3c-972a-438be0be593a` |
| `follow-1002556d-d430-4182-b623-0ae4bf5d6dd7` | `b7c2c451-e1a4-4262-86c8-a33560c8d374` |

ทุกรายการส่งด้วย `Idempotency-Key` เป็น `follow-<id>` ซึ่งไม่ซ้ำกันระหว่างรายการ
และเวลานัดที่ส่งไปเป็นเวลาอนาคตทุกรายการ (ห่างจากเวลาที่ส่ง 3–9 นาที)

ขอบคุณมากครับ

---

## เพิ่มเติม 11 กันยายน 2569 — หลักฐานชัดขึ้น

วันนี้เก็บข้อมูลได้อีกชุด ขอเพิ่มรายละเอียดของ **เรื่องที่ 2** ครับ

เราพบรูปแบบที่ชัดมาก: เวลาส่งสายสองรอบของคนเดียวกันเข้าไปพร้อมกัน
(สร้างในนาทีเดียวกัน หมายเลขเดียวกัน เวลานัดห่างกัน 3–5 นาที)
**สายที่นัดไว้ทีหลังคือสายที่ได้ผลกลับ ส่วนสายที่นัดไว้ก่อนมักไม่ได้ผลเลย**

| สายในชุด | ส่งเข้าไป | ได้ผลกลับ |
| --- | --- | --- |
| สายที่นัดไว้ทีหลัง | 16 | 7 |
| สายที่นัดไว้ก่อน | 16 | 1 |

ตัวอย่างที่ชัดที่สุดของวันนี้ — คนเดียวกัน หมายเลขเดียวกัน สร้างพร้อมกันเวลา 10:07 น.

| `client_contact_id` | เวลานัด | ผล |
| --- | --- | --- |
| `follow-3dbac9fb-8bdf-41ab-8468-2801b481dd8e` | 10:20 | ไม่มีผลกลับเลย |
| `follow-5ffc9ea0-f29b-4c62-ad8a-ad89ad8688e6` | 10:25 | `confirmed` ปกติ |

รายการแรกส่งเข้าไปด้วย `client_contact_id` ที่ไม่ซ้ำ เวลานัดเป็นอนาคต (ล่วงหน้า 13 นาที)
และ `Idempotency-Key` คนละตัวกับรายการที่สอง

จึงอยากขอให้ช่วยตรวจว่า **รายการที่สองไปแทนที่รายการแรกของหมายเลขเดียวกันหรือไม่**
ถ้าใช่ รบกวนแนะนำวิธีที่ถูกต้องสำหรับการนัดหลายสายต่อหนึ่งหมายเลขในวันเดียวกันด้วยครับ
(เราตั้งใจให้เป็นคนละสาย คนละเวลา ไม่ได้ต้องการให้ทับกัน)

## บันทึกภายใน (ไม่ต้องส่ง)

**ฝั่งเราก็มีจุดบอดที่แก้แล้วในรอบนี้** — `pushFollowReminderToLumos()` เป็น
fire-and-forget: ตั้ง `dispatch_state = 'queued'` ตั้งแต่ก่อนรู้ผล push
ถ้า HTTP ไปไม่ถึง Lumos จะเห็นแค่ log ส่วนบนจอขึ้นว่า "รอ AI โทร" ตลอดไป
⇒ แยกไม่ออกระหว่าง "ส่งไม่ถึง" กับ "ส่งถึงแล้วแต่เขาเงียบ"
เพิ่มสถานะ `push_failed` แล้ว (ดู `09-editing-map.md` หัวข้อ 10 ก.ย. 2569)

**สิ่งที่ยังพิสูจน์ไม่ได้จากฝั่งเรา** — วันที่ 9 ก.ย. ไม่มีหมายเลขไหนเลยที่มีรายการเดียว
(ทุกหมายเลขถูกใช้ซ้ำ 2–5 รายการ) จึงแยกไม่ออกว่าเป็นเรื่องหมายเลขชนกันจริง
หรือเป็นอย่างอื่น · ถ้าจะพิสูจน์เองต้องทดสอบด้วยหมายเลขที่ไม่ซ้ำกัน
ซึ่งแปลว่าโทรหาเบอร์จริง — ต้องให้เจ้าของสั่งก่อน
