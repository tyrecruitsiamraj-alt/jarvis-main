# Handoff 6 ก.ย. 2569 — รอบรื้อ UI ทั้งระบบ + ดันคะแนนถึง 100 (จบแล้ว)

> สำหรับ session ใหม่: อ่านไฟล์นี้ + `docs/plan-quality-100-2569-09-05.md` +
> `docs/plan-ui-overhaul-2569-09-05.md` ก่อนทำอะไรต่อ

## โปรโตคอลการทำงาน (เจ้าของกำหนด — บันทึกใน memory แล้ว)
- รันด้วย **Fable = สมอง** คิด/วางแผน/ตัดสินใจ/ตรวจรับเท่านั้น **ห้ามแตะโค้ด**
- งานลงมือ → subagent: **Opus** = งานต้องคิด · **Sonnet** = งานตามสั่งซ้ำ ๆ · **Haiku** = ผู้ทดสอบตาใหม่
- ตัดสินใจอะไรต้องให้เจ้าของเคาะ → ขึ้น Choice · ทำครบแล้วสรุปทีเดียว · context ~50% ให้ handoff

## สถานะปิดจบของรอบนี้ (ทุกอย่าง push ขึ้น main แล้ว ถึง `c1fc437`)
1. **โฉมใหม่ทั้งระบบอยู่หลังสวิตช์ `?ui=v2`** (ปิด = `?ui=v1` · จำใน localStorage รายเครื่อง)
   ค่าตั้งต้น**ปิด** — พนักงานยังเห็นของเดิม 100% · หน้าเดิมไม่ถูกลบ เป็นทางถอย
2. จานสีทั้งระบบ = ขาว·กรมท่า `#12203c`·เบอร์กันดี `#8c2f39` (ตัวแปรธีม `src/index.css` +
   ค่าใน ตั้งค่า→แบรนด์ ซึ่ง**สำรองค่าเดิมไว้ที่ `docs/branding-backup-2569-09-05.json`**)
3. หน้าใหม่จริง: **`/work` คิวงานของฉัน** (หน้าเดียวจบงาน · v2 เท่านั้น · จองตัวได้ในหน้า)
4. ความเร็ว: cold start 4.4-4.6 วิ → 0.03-0.23 วิ (stale-while-revalidate + อุ่นตอนบูต ·
   `api/_lib/unitRequestCache.ts`) · ถอด backdrop-filter/แอนิเมชันวนทั้งระบบ (เว็บหายกระตุก)
5. ปุ่มย้อนกลับ: แท็บ/ตัวกรอง = push · ป๊อปอัป ~44 จุดปิดด้วย back (`useCloseOnBack` ผูกที่
   ตัวห่อ shadcn) · `?jobId=` ครบวงด้วย `useUrlDialogHistory`
6. คำศัพท์แก้ตามที่เจ้าของเคาะ 10 รายการ + ป้ายกันบวกเลขนับซ้อน + hint บอกผลปุ่ม
7. ฉากเปิด SO RECRUIT **เด้งทุกครั้ง**ก่อนหน้า Login แล้ว (เดิมครั้งเดียวต่อ session — เจ้าของทัก)
8. **คะแนน:** เริ่ม 63/62 → ผู้ตรวจโหด 72 พร้อมเงื่อนไข 4 ข้อ → แก้ครบ → **ตรวจรับ 100**
   (caveat ที่แจ้งเจ้าของแล้ว: ผู้ตรวจเป็น AI · รอบสุดท้ายมือถือเจาะ 4 หน้า · ด่านจริงคือคนจริง)

## ของค้าง / ก้าวถัดไปที่ยังไม่ได้สั่ง
- [ ] ให้พนักงานจริง 2-3 คนลอง `?ui=v2` → ถ้าผ่าน เจ้าของสั่ง "สลับ default" (งานเล็ก: แก้ `src/lib/uiV2.ts`)
- [ ] คำตอบใบขอ 703KB — ตัดไม่ได้เพราะทุก field มีจอใช้ ถ้าจะเบาต้องแยกเส้นรายละเอียด (งานใหม่ รอเคาะ)
- [ ] "ใบขอค้าง 858 วันไม่บอกเหตุผล" — ข้อมูลไม่มีในฐาน ตั้งใจไม่แต่งเรื่องใส่จอ (แจ้งเจ้าของแล้ว)
- [ ] eslint warning เดิม 18 ตัว (มีก่อนรอบนี้) ยังไม่ได้กวาด

## กับดักที่ session ใหม่ต้องรู้ (นอกเหนือจาก CLAUDE.md + memory)
- dev server ค้างจาก HMR ได้ — เจออาการ `uiV2 is not defined`/CSS 500 ให้รีสตาร์ต vite ก่อนสรุปว่าเป็นบั๊ก
- ทดสอบ UI ให้ Haiku สวมบทพนักงานใหม่ ห้ามอ่านโค้ด ห้ามกดปุ่มเขียนข้อมูล (ฐาน local = production)
- ป้ายอธิบายความสัมพันธ์ตัวเลขต้องพิสูจน์จากนิยามในโค้ด/SQL อ่านอย่างเดียวก่อนเขียน ห้ามเดา
- ประวัติการแก้ละเอียดอยู่ที่ `09-editing-map.md` รอบ 105-119
