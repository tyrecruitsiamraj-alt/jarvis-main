# Prompt สำหรับเปิด session ใหม่ — jarvis / So Recruit

คัดลอกทั้งบล็อกข้างล่างไปวางเป็นข้อความแรกของ session ใหม่

---

```
ทำงานต่อโปรเจกต์ jarvis / So Recruit

อ่านตามลำดับก่อนลงมือ ห้ามข้าม:
1. docs/SESSION-HANDOFF.md — หัวบนสุด "รอบเจ็ดสิบเอ็ด"
   (มีวิธีคุยที่ฉันสั่งไว้ + บทเรียน 5 ข้อที่นายเคยพลาด + สเปคที่ยังไม่ทำ 9 ข้อ)
2. .claude/skills/request-control-tower-advisor/references/09-editing-map.md
   ส่วน "รอบเจ็ดสิบ" กับ "รอบเจ็ดสิบเอ็ด"
3. หัวไฟล์ src/lib/boardRelease.ts (กติกาเลขของกล่องงาน)

🔴 วิธีคุย: เอาภาษามนุษย์ บอกว่าของบนจอเปลี่ยนไปยังไง ไม่ต้องพ่นชื่อไฟล์
ไม่ต้องยัดตัวเลขเทสต์ทุกข้อความ สั้นเข้าไว้ · คิดเองแล้วทำให้ดู
แต่เรื่องที่ตัดสินแทนฉันไม่ได้ ให้ถามเป็น Choice — ห้ามเดา

สถานะ: งาน 3 ก้อนของรอบเจ็ดสิบเอ็ด commit + push ขึ้น main แล้ว
(เลิกใช้เลขขั้น · กล่องงาน 3 ก้อน + ป๊อปไล่งาน 1-4 · ถอด ✓ / กางใบขอในกล่อง /
ประวัติแบ่งหน้า) — ตรวจบนจอจริงครบทั้งสามก้อนแล้ว working tree สะอาด

🔴 เรื่องค้างที่ต้องเคาะก่อน:
หน้ากล่องงานโหลดไม่ติดบ่อย — เส้นดึงใบขอจากระบบงานหลักค้างครบ 60 วิแล้วตาย
(เจอ 4 ใน 6 ครั้ง คืน 28 ส.ค. · ครั้งที่ผ่านใช้ 10 วิ · ระบบงานหลักตอบ ping ปกติ)
ตอนตายหน้าจอขึ้นเลข 0 ทุกช่องเหมือนเป็นเลขจริง มีแค่บรรทัดแดงเล็ก ๆ ว่า Timeout
⇒ ตรงกับหนี้ Redteam ข้อ 1 "หน้าจอโกหกทั้งหน้า" แต่รอบนี้เกิดจากเส้นช้า ไม่ใช่สิทธิ์
ยังไม่ได้แก้ รอฉันเคาะ

งานถัดไปเลือกจาก:
- แก้ให้ตอนโหลดไม่สำเร็จขึ้นว่า "อ่านตัวเลขไม่ได้" แทนที่จะโชว์ 0
- สเปคที่ยังไม่ทำ 9 ข้อ (ตารางใน SESSION-HANDOFF.md)
- หนี้ Redteam 5 ข้อ — ข้อ 5 (/apply โชว์ชื่อคนที่ลาออก) อยู่บน production ควรแก้เร็วสุด
```

---

## วิธีตรวจงานบนจอ (ที่ได้ผลจริง)

* ฐาน local ชี้ **DB จริง** · ล็อกอินหมดอายุ 30 นาที ตรวจจอต้องรีบ
* ถ้าเปิดแล้วทุกอย่างขึ้น 500 = ตัวเชื่อมข้อมูลตาย ให้ kill โปรเซส `server/local-api.ts`
  แล้วสตาร์ตใหม่ด้วย `npm run api:local`
* 💡 **ส่งโมเดลอ่อนสุด (Haiku) สวมบทพนักงานใหม่มาเล่นหน้าจอ** ห้ามอ่านโค้ด
  ให้ดูจากจอเท่านั้น + บังคับกดจริง แล้วให้ตอบหัวข้อคงที่
  (เข้าใจอะไร · งงอะไร · ศัพท์ไหนไม่รู้ · **ถ้าไม่มีใครห้าม จะกดปุ่มสุดท้ายด้วยความมั่นใจแค่ไหน 1-10**)
  → จับบั๊กระดับหน้าจอที่เทสต์ 2,462 ข้อมองไม่เห็น
