-- 114 · ชื่อเล่น + สายงาน บนบัญชีผู้ใช้ — ตั้งสิทธิ์/ชื่อเล่น/เบอร์ ที่เดียวจบ
--
-- เจ้าของสั่ง 1 ก.ย. 2569: *"ถ้าฉันเอาตำแหน่งพวกนี้มาเพิ่มพร้อมเบอร์โทรในหน้าผู้ใช้งาน
-- จะง่ายกว่าไหม มันจะได้กำหนดทั้ง Role คัดสรร ฯลฯ ชื่อเล่น และเบอร์โทรทีเดียว"*
--
-- 🔴 **ไม่แตะ `job_staff_roster`** (เจ้าของสั่งเอง: *"หน้าคัดสรร สรรหา ปล่อยไว้แบบนั้นก่อน
-- เพราะตอนนี้ใช้งานอยู่ ถ้าลบเดี๋ยวกระทบใบขอ"*) — ตารางนั้นยังเป็นแหล่งของ dropdown ทั้งระบบ
-- ที่เพิ่มตรงนี้คือ **ที่เก็บชุดใหม่** ให้คนทยอยกรอก แล้วอนาคตค่อยย้าย dropdown มาอ้างที่นี่แทน
--
-- ⚠️ **ชื่อในสองที่ไม่ตรงกันเลยสักชื่อ** (วัดจริง 1 ก.ย. 2569: roster 41 ชื่อเป็นชื่อเล่นไทย
-- ส่วน users 55 บัญชีเป็นชื่ออังกฤษจาก Microsoft · จับคู่อัตโนมัติได้ 0 คู่)
-- ⇒ การย้ายในอนาคตต้องให้คนจับคู่เอง ระบบเดาให้ไม่ได้ · คอลัมน์นี้คือที่รองรับตอนนั้น
--
-- ⚠️ **ไม่ใส่ CHECK บน job_lanes** — บ้านนี้โดน CHECK ล็อกค่าใหม่มาสองรอบแล้ว
-- (source / result_scope) · ตรวจค่าที่ handler แทน
--
-- ค่าที่ใช้ใน job_lanes: recruiter (สรรหา) · screener (คัดสรร) · opl · online
-- null / อาเรย์ว่าง = ยังไม่ได้ตั้ง — จอต้องบอกว่า "ยังไม่ตั้ง" ห้ามเดาว่าไม่มีสายงาน

alter table users
  add column if not exists nickname text,
  add column if not exists job_lanes text[];

comment on column users.nickname is
  'ชื่อเล่น (ไทย) — ชื่อที่ทั้งระบบเรียกกันจริง · full_name เป็นชื่ออังกฤษจาก Microsoft ซึ่งไม่มีใครใช้เรียก';
comment on column users.job_lanes is
  'สายงานที่คนนี้อยู่: recruiter/screener/opl/online · null = ยังไม่ได้ตั้ง (คนละเรื่องกับ role ที่เป็นสิทธิ์ในระบบ)';
