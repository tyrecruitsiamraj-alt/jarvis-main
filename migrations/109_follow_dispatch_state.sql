-- จดไว้ว่ารายการติดตามนี้ "ถูกส่งให้ AI โทรไหม และถ้าไม่ เพราะอะไร"
--
-- 🔴 ที่มา (เจ้าของสั่ง 25 ส.ค. 2569): รายการติดตามที่สร้างเมื่อ 24 ส.ค. 16:01
-- **ไม่ถูกส่งให้ AI และไม่มีอะไรบอกใครเลย** — หน้าจอขึ้นว่าสร้างสำเร็จ
-- คนสร้างนั่งรอสายที่ไม่มีวันออก · กว่าจะรู้ต้องไล่ฐานย้อนหลังทีละตาราง
--
-- ระบบมีหลายทางที่จะ "ไม่ส่ง" โดยตั้งใจและถูกต้อง (มีคนจองเบอร์ · เบอร์ห้ามโทร ·
-- ตรวจบัญชีห้ามโทรไม่ได้ · ไม่มีเบอร์ · ปิดส่งอัตโนมัติ) แต่ **ไม่เคยจดไว้ที่ไหน**
-- ⇒ ผลตอนส่งอยู่แค่ใน log ของ server ซึ่งคนใช้งานเข้าไม่ถึง
--
-- คอลัมน์นี้เก็บผลตอนพยายามส่ง **ครั้งแรกตอนสร้าง** เท่านั้น
-- สถานะหลังจากนั้น (AI รับไปโทร/โทรจบ) อ่านจาก lumos_dispatch_queue เหมือนเดิม
-- ค่าที่เป็นไปได้อยู่ที่ src/lib/followDispatchState.ts (FOLLOW_DISPATCH_STATES)
--
-- ⚠️ null = แถวเก่าที่สร้างก่อนมีคอลัมน์นี้ ⇒ **ไม่รู้ว่าทำไม** ห้ามตีความว่า "ส่งแล้ว"
--    (จอต้องบอกตรง ๆ ว่าไม่มีบันทึก — ดู followDispatchLabel())
-- ⚠️ ไม่ใส่ CHECK constraint ไว้ตั้งใจ — ค่าชุดนี้ยังมีโอกาสเพิ่มถังใหม่
--    และกับดักเดิมของโปรเจกต์นี้คือ CHECK ที่ลืมแก้คู่กับโค้ดแล้วเซฟไม่ผ่าน 500 เงียบ ๆ
--    (เจอมาแล้วสองรอบกับ source/result_scope) · ฝั่งอ่านกันค่าเพี้ยนด้วย isFollowDispatchState

ALTER TABLE follow_entries
  ADD COLUMN IF NOT EXISTS dispatch_state text NULL;

COMMENT ON COLUMN follow_entries.dispatch_state IS
  'ผลตอนพยายามส่งเข้าคิว AI ตอนสร้าง (queued/held/suppressed/guarded/no_phone/off) · null = แถวเก่า ไม่รู้';
