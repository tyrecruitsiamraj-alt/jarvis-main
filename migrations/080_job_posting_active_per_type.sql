-- ผ่อนกติกา "หนึ่งใบขอ = หนึ่งคำขอโพสที่ยัง active" ให้เป็น "หนึ่งใบขอ = หนึ่งคำขอต่อประเภท"
--
-- เจ้าของสั่ง 13 ส.ค. 2569: "เลือกไปสร้าง Content หรือ Scraping ตอนนี้ถ้าเลือกอย่างใด
-- อย่างนึงแล้วมันหายไปเลย ไม่ควรไหม ควรจะเลือกอันไหนอันนั้นก็ซ่อน แล้วโชว์อันที่ยังไม่ได้เลือกไว้"
--
-- ของเดิมกันซ้ำระดับ "ใบขอ" เฉย ๆ → กด Content ไปแล้ว กด Scraping จะได้คำขอ Content
-- เดิมกลับมาแบบเงียบ ๆ (createJobPostingRequest คืนตัวที่มีอยู่) = ปุ่มที่กดแล้วไม่เกิดอะไร
-- ซึ่งแย่กว่าปุ่มหาย เพราะคนกดแล้วคิดว่าส่งไปแล้ว
--
-- index ใหม่กว้างกว่าเดิมเสมอ (คู่ที่ผ่านเงื่อนไขเดิมย่อมผ่านเงื่อนไขใหม่) จึงไม่มีทาง
-- ล้มเพราะข้อมูลเดิมชนกัน · ⚠️ ทิศทางเดียวเท่านั้น — ถอยกลับต้องเคลียร์คำขอที่ซ้ำใบก่อน

drop index if exists job_posting_requests_active_job_idx;

create unique index if not exists job_posting_requests_active_job_type_idx
  on job_posting_requests (job_id, request_type)
  where status in ('pending', 'in_progress', 'posted');
