# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# ความลับ — กรอกบนเครื่องคุณเท่านั้น (DB, JWT, ฯลฯ) ห้าม commit
.env
.env.production
.env.vercel.production

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.vercel

# Machine-local mirror of .claude/skills (see AGENTS.md) — do not commit
.agents/

# ไฟล์สำรองก่อนล้างคิวโทร (มี person_ref ของผู้สมัครจริง) — เก็บไว้บนเครื่อง ห้าม commit
lumos-queue-cancel-backup-*.json
wipe-backup-*.json

# Skill ที่ลงจาก `npx skills add` — ไฟล์จริงอยู่ใน .agents/ (machine-local)
# ส่วนใน .claude/skills เป็น **symlink** ชี้เข้าไป ถ้า commit จะได้ symlink เสีย
# ลงใหม่บนเครื่องอื่นด้วย: npx skills add shadcn/ui   (เวอร์ชันล็อกที่ skills-lock.json)
.claude/skills/shadcn
.claude/skills/migrate-radix-to-base
