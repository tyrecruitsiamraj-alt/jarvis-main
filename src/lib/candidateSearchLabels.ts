/**
 * คำบนปุ่ม "หาคนเพิ่ม" ทั้ง 3 ทาง — **ที่เดียวของทั้งระบบ**
 *
 * เจ้าของเคาะ 22 ส.ค. 2569 (Phase 3 ข้อ 3.2): *"เก็บทั้ง 3 ปุ่ม แค่เปลี่ยนคำให้ต่างกันชัด"*
 * ปัญหาเดิม: สามปุ่มนี้ผลลัพธ์คล้ายกันจนคนใหม่แยกไม่ออกว่าต้องกดอันไหน
 *   ① "ค้นหาคนที่ยังไม่สมัคร"   (ฐานเก่า · ไม่โทร)
 *   ② "หาคนเพิ่ม + ส่ง AI โทร"  (ฐานเก่า · โทรทันที)
 *   ③ "หาผู้สมัครเพิ่ม"         (ทุกฐาน · ไม่โทร)
 *
 * 🔴 กติกาของคำในไฟล์นี้:
 * 1. **ห้ามใส่ชื่อระบบ ("iRecruit") ในปุ่ม** — เจ้าของสั่งไว้ตั้งแต่ 16 ส.ค. 2569 ว่าให้ใช้คำ
 *    ที่บอกว่า "ได้อะไร" ไม่ใช่ชื่อฐานข้อมูล
 * 2. คำต้องแยกให้ออก **2 มิติ** ที่ต่างกันจริง: ค้นจาก *กองไหน* · และ *โทรเลยหรือไม่*
 * 3. ปุ่มที่ยิงสายจริงต้องมีคำว่า "โทร" อยู่ในปุ่ม + มี popup ยืนยัน (Phase 4 ข้อ 4.2)
 */

/** ① ค้นกองคนที่เคยฝากประวัติไว้ (ฐานเดิม) — ไม่โทร แค่เอารายชื่อมาดู */
export const SEARCH_LEGACY_POOL = {
  label: 'ดูคนที่ยังไม่สมัครงานนี้',
  again: 'ดูรายชื่อใหม่อีกครั้ง',
  hint: 'ค้นจากกองคนที่เคยฝากประวัติไว้ — ได้รายชื่อมาดูก่อน ยังไม่โทรหาใคร',
} as const;

/** ② กองเดียวกับ ① แต่ส่งเข้าคิวให้ AI โทรทันที */
export const SEARCH_LEGACY_POOL_AND_CALL = {
  label: 'ให้ AI โทรหาคนที่ยังไม่สมัคร',
  hint: 'ค้นจากกองเดียวกับปุ่มซ้าย แล้วส่งคนที่ AI แนะนำเข้าคิวโทรทันที (โทรออกแล้วเรียกคืนไม่ได้)',
} as const;

/** ③ ค้นทุกกองที่ระบบมี (ฐานเดิม + ฐานใหม่ + ถังคัดกรอง) — ไม่โทร */
export const SEARCH_ALL_POOLS = {
  label: 'หาผู้สมัครเพิ่มจากทุกกอง',
  again: 'ค้นทุกกองอีกครั้ง',
  hint: 'ค้นทุกกองที่ระบบมี (คนฝากประวัติเดิม · ผู้สมัครใหม่ · ถังคัดกรอง) — ไม่โทรหาใคร',
} as const;

/**
 * ④ กองเดียวกับ ③ (ทุกกอง) **แต่ส่งเข้าคิวโทรทันที** — ปุ่มบนการ์ดของบอร์ดรับสมัคร
 *
 * 🔴 นี่คือปุ่มที่อันตรายที่สุดในสามสี่ปุ่มนี้ และเคยใช้ **คำเดียวกับ ③** ทั้งที่พฤติกรรมตรงข้าม
 * (เจอจริงตอนทำ Phase 3 · 23 ส.ค. 2569: การ์ดบอร์ดเรียก `RecruitLaneDialog` ด้วย
 * `send: true` = โทรทันทีที่เปิด · ส่วนแท็บ AI Match เรียก `send: false` = ดูเฉย ๆ
 * แต่ทั้งสองที่เขียนบนปุ่มว่า "หาผู้สมัครเพิ่ม" เหมือนกันเป๊ะ)
 * → คำต้องบอกว่าโทร และต้องมีขั้นยืนยันก่อนยิงสายเสมอ
 */
export const SEARCH_ALL_POOLS_AND_CALL = {
  label: 'หาคนทุกกอง + ให้ AI โทร',
  hint: 'ค้นทุกกองที่ระบบมี แล้วส่งคนที่ AI แนะนำ (เขียว/เหลือง) เข้าคิวโทรทันที — โทรออกแล้วเรียกคืนไม่ได้',
} as const;
